# GraphQL endpoint. Defaults to "/graphql", which is served by the built-in
# MSW mock layer (see src/mocks) so the app runs standalone with no backend.
# Point this at a real GraphQL API to go live.
VITE_GRAPHQL_API_URL=/graphql
